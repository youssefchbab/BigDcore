\section{Introduction}
This chapter describes the VHDL implementation of RV32ICore and its simulation-based verification. Each architectural unit from Chapter 2 is realized as a distinct VHDL entity. Functional correctness is verified by compiling C programs of increasing complexity using the xPack RISC-V GCC toolchain, executing them in simulation, and comparing memory/register results against analytically computed expected values.

\section{VHDL Design Organization}
RV32ICore is organized as a hierarchy of VHDL entities. The top-level entity \textit{P\_Processor\_Hazard} instantiates the datapath \textit{(P\_Datapath)}, control unit \textit{(P\_Control\_Unit)}, and hazard unit \textit{(Hazard\_Unit)}. Memory is kept external to the processor entity, connected through top-level ports, allowing different memory configurations without modifying internal logic.

Top-level ports: clock, reset, 32-bit instruction input, 32-bit read data input, and outputs for PC, data address, write data, memory write enable, byte enable, and access mode.

\section{Pipeline Register Implementation}
All pipeline registers are implemented using a single generic VHDL component \textit{RegEn}, parameterized by data width. It supports four modes: normal clocked operation, stall (output held), flush (NOP inserted), and synchronous reset.

The NOP value is \texttt{x"00000033"} (\textit{ADD x0, x0, x0}) — a valid instruction that writes to x0, which is hardwired to zero, avoiding undefined behavior in decode/control logic during pipeline recovery. Narrower variants (\textit{RegEn\_5bit}, \textit{RegEn\_9bit}, \textit{RegEn\_6bit}) handle register addresses and reduced control buses.

\section{Control Unit Implementation}
\textit{P\_Control\_Unit} contains three sub-components:

\textbf{P\_Main\_unit} — combinational decoder mapping opcode and Funct3 to all datapath control signals (immediate select, memory width, register/memory write enables, write-back select, PC select, ALU input select, branch mode).

\textbf{Alu\_cntr} — decodes opcode, Funct3, and Funct7[5] to produce the 4-bit ALU operation code. Funct7[5] alone distinguishes arithmetic pairs (ADD/SUB, SRL/SRA).

\textbf{Excution\_Flow} — resolves the final PC select in EX from the pipelined PC select, branch mode, ALU zero flag, and ALU LSB, producing a 2-bit output selecting PC+4, ALU result (JALR), or branch target.

\subsection{Control Bus Pipelining}
All control signals are packed into a single 21-bit bus at the Decode stage output and pipelined through \textit{RegEn} registers in parallel with the datapath. At each stage boundary, only the relevant subset is extracted. The bus narrows progressively: 21-bit → 9-bit entering MEM (memory and write-back controls) → 6-bit entering WB (register write enable, memory extension, write-back select).

\section{Datapath Implementation}

\subsection{Fetch Stage}
The PC is a \textit{RegEn} instance with stall input connected to \textit{StallF}. \textit{Pc\_Next} is selected by \textit{Mux3} among: ALU result (JALR), PC+4 (sequential), and branch target (taken branch). PC and PC+4 are registered into IF/ID for later stages.

\subsection{Decode Stage}
The register file is read combinationally using rs1[19:15] and rs2[24:20]. rd[11:7] is propagated through a dedicated 5-bit pipeline register chain to WB. The immediate extension unit takes the full instruction word and a 3-bit format select, producing a 32-bit sign-extended immediate. rs1 and rs2 are also registered separately as Rs1E and Rs2E for hazard unit comparisons.

\subsection{Execute Stage}
Forwarding multiplexers \textit{ForwardingA} and \textit{ForwardingB} (Mux3 instances) select among: register file output, WB result, or EX/MEM ALU result, controlled by 2-bit \textit{ForwardA/ForwardB} from the Hazard Unit. The forwarded rs2 value is also registered into MEM via \textit{ForwaredB\_PLR4} for use by Store instructions. ALU input A is selected by \textit{Mux3} (forwarded rs1, PC for AUIPC, or zero for LUI); ALU input B by \textit{Mux2} (sign-extended immediate or forwarded rs2). The branch target is computed as the Decode-stage PC plus the sign-extended immediate.

\subsection{Memory Stage}
The data address is the EX/MEM ALU result. Write data is from \textit{ForwaredB\_PLR4}. The two LSBs of the ALU result are passed to the top level as \textit{ByteEn} for sub-word memory access.

\subsection{Write-Back Stage}
\textit{Mem\_SignExt} sign- or zero-extends byte/halfword load results per Funct3 and the ALU result LSBs. The WB multiplexer selects among PC+4 (JAL/JALR), extended memory data (loads), or ALU result (all others), writing to the register file at the address in \textit{Reg\_A3\_PLR5}.

\section{Hazard Unit Implementation}
\textit{Hazard\_Unit} is fully combinational, receiving source/destination register addresses from all pipeline stages, register write enables, write-back control LSB, and PC select MSB.

\subsection{Forwarding Logic}
MEM-stage forwarding takes priority over WB-stage forwarding (most recent value wins). Forwarding is suppressed when the destination is x0. Forwarding is also enabled when the PC select LSB is \texttt{'0'}, capturing JAL/JALR instructions that write PC+4 but are not flagged by the standard \textit{RegWrite} path.

\subsection{Load-Use Stall}
Detected when the EX-stage instruction is a load (\textit{Writeback\_cntr} LSB asserted) and its destination matches either source register in Decode. Response: assert \textit{StallF} and \textit{StallD} to freeze IF/ID and ID/EX, assert \textit{FlushE} to insert a NOP into EX for one cycle.

\subsection{Control Hazard Flush}
\textit{FlushE} = load-use stall OR PC select MSB. \textit{FlushD} = PC select MSB alone. This discards the two incorrectly fetched instructions while leaving the Fetch stage free to begin fetching from the branch target.

\section{Toolchain and Program Loading}
Test programs are compiled using \textit{riscv-none-elf-gcc} targeting RV32I (no compressed instructions or floating-point). A custom linker script places the text segment at \texttt{0x00000000} matching the reset vector, with the stack pointer initialized to the top of data memory. A startup assembly file sets the stack pointer and zeros the BSS section before transferring control to \texttt{main}.

\begin{minted}{c}
	int sum_array(int arr[], int len) {
		int sum = 0;
		for (int i = 0; i < len; i++) sum += arr[i];
		return sum;
	}
	int main() {
		int arr[] = {3, 7, 2, 9, 1};
		int s = sum_array(arr, 5); // expected: 22
		return 0;
	}
\end{minted}

\begin{minted}{riscv}
	00000000 <_start>:
	0:  40000113   li   sp, 1024
	4:  11400513   li   a0, 276
	8:  11400593   li   a1, 276
	0000000c <bss_loop>:
	c:  00b55863   bge  a0, a1, 1c <bss_done>
	10:  00052023   sw   zero, 0(a0)
	14:  00450513   addi a0, a0, 4
	18:  ff5ff06f   j    c <bss_loop>
	0000001c <bss_done>:
	1c:  07c000ef   jal  98 <main>
	00000020 <halt>:
	20:  0000006f   j    20 <halt>
\end{minted}
\textbf{Note:} Full compiled output is provided in the Appendix.

The output ELF is converted to a hex file and loaded into simulation memory via:
\begin{minted}{text}
	mem load -infile output.hex -format hex
	sim:/hazard_testbench/Inst_RAM/Mem
\end{minted}

\section{Simulation Environment}
\subsection{Testbench}
\begin{minted}{vhdl}
	Library ieee;
	use ieee.std_logic_1164.all;
	entity Hazard_Testbench is end entity;
	
	architecture arch of Hazard_Testbench is
	component P_Processor_Hazard is port(...); end component;
	component RAM_Inst is port(...); end component;
	component RAM is port(...); end component;
	signal clk, reset : std_logic;
	signal instruction, ReadData : std_logic_vector(31 downto 0);
	signal MemWrite : std_logic;
	signal ByteEn, Mode : std_logic_vector(1 downto 0);
	signal Pc, DataAdr, WriteData : std_logic_vector(31 downto 0);
	signal clk_cnt, Inst_Change_Cnt : integer := 0;
	begin
	UUT:      P_Processor_Hazard port map(...);
	Data_RAM: RAM                 port map(...);
	Inst_RAM: RAM_Inst            port map(...);
	
	clock: process begin
	loop clk <= '1'; wait for 10 ns;
	clk <= '0'; wait for 10 ns;
	end loop;
	end process;
	
	Clock_counter: process(clk)
	variable last : std_logic_vector(31 downto 0);
	begin
	if rising_edge(clk) then
	clk_cnt <= clk_cnt + 1;
	if instruction /= last then Inst_Change_Cnt <= Inst_Change_Cnt + 1; end if;
	last := instruction;
	end if;
	end process;
	
	Stimulus: process begin
	reset <= '1'; wait for 40 ns;
	reset <= '0'; wait;
	end process;
	end architecture;
\end{minted}

The testbench applies reset for two clock cycles, initializing PC, register file, and memory to zero. A 20\,ns clock is generated via wait commands. \textit{Inst\_Change\_Cnt} counts distinct instructions executed, used to measure performance.

\section{Verification Tests}

\subsection{Test 1 — Sum Array}
After reset, the PC initializes to 0 and increments by 4 each cycle. Figure~\ref{fig:waveform} shows correct initialization behavior.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{"../../Images to be used/Waveform"}
	\caption{RV32ICore Initialization}
	\label{fig:waveform}
\end{figure}

At program completion, the result 0x16 (22) is stored at address \texttt{0x000003BC} (word index 0xEF in data memory), consistent with the expected sum of \{3,7,2,9,1\}.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{"../../Images to be used/Sum array result"}
	\caption{Sum Array Result}
	\label{fig:sum-array-result}
\end{figure}

\subsection{Test 2 — Multi-Function}
A second test exercises sum\_array, a multiplication-by-repeated-addition factorial, pointer-based find\_max, and bitwise count\_bits. Expected results: 0x16 (22), 0x78 (120), 0x09 (9), 0x08 (8).

\begin{minted}{c}
	int factorial(int n) {
		int result = 1;
		for (int i = 2; i <= n; i++) {
			int temp = 0;
			for (int j = 0; j < i; j++) temp += result;
			result = temp;
		}
		return result;
	}
	int find_max(int arr[], int len) {
		int max = arr[0];
		for (int i = 1; i < len; i++)
		if (arr[i] > max) max = arr[i];
		return max;
	}
	unsigned int count_bits(unsigned int n) {
		unsigned int count = 0;
		while (n) { count += n & 1; n >>= 1; }
		return count;
	}
	int main() {
		int arr[] = {3, 7, 2, 9, 1};
		int s = sum_array(arr, 5);        // 22
		int f = factorial(5);              // 120
		int m = find_max(arr, 5);         // 9
		unsigned int b = count_bits(255); // 8
		return 0;
	}
\end{minted}

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.7\linewidth]{"../../Images to be used/Second Test"}
	\caption{Second Test Results}
	\label{fig:second-test}
\end{figure}

All four results match expectations. Remaining RV32I instructions were verified through targeted test programs.

\section{Conclusion}
This chapter presented the complete VHDL implementation of RV32ICore and its simulation-based verification. Key implementation decisions — the generic \textit{RegEn} component, progressive control bus narrowing, \texttt{ADD x0,x0,x0} as the NOP encoding, and the dedicated forwarded rs2 register for stores — reflect deliberate trade-offs between correctness, clarity, and reuse. Verification confirmed correct reset behavior, full RV32I instruction execution, and accurate results across both test programs, including loop execution, recursion, pointer manipulation, and bitwise operations.